import React from "react";
import styled from "styled-components";

const Wrapper = styled.div`
  align-items: center;
  text-align: center;
`;

export default function WrapperCenter() {
  return <Wrapper></Wrapper>;
}
